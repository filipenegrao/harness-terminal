{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — grants core IPC including event listen/emit for PTY bridge",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
